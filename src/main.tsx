import React,{useEffect,useState} from 'react';
import {createRoot} from 'react-dom/client';
import {CalendarDays,CheckCircle2,ChevronLeft,ChevronRight,FileText,LayoutDashboard,Menu,Plus,Search,Settings,Sun,Moon,Trash2,Pin,PinOff,X,Pencil} from 'lucide-react';
import './styles.css'; import type {Note,Priority,Task} from './types'; import {loadNotes,loadTasks,saveNotes,saveTasks} from './store';
type Page='Prehľad'|'Úlohy'|'Poznámky'|'Kalendár'; const nav:[Page,typeof LayoutDashboard][]=[['Prehľad',LayoutDashboard],['Úlohy',CheckCircle2],['Poznámky',FileText],['Kalendár',CalendarDays]];
function App(){const[page,setPage]=useState<Page>('Prehľad'),[tasks,setTasks]=useState(loadTasks),[notes,setNotes]=useState(loadNotes),[dark,setDark]=useState(()=>localStorage.getItem('planora_theme')==='dark'),[query,setQuery]=useState(''),[modal,setModal]=useState<{kind:'task'|'note';item?:Task|Note}|null>(null),[menu,setMenu]=useState(false);useEffect(()=>saveTasks(tasks),[tasks]);useEffect(()=>saveNotes(notes),[notes]);useEffect(()=>{localStorage.setItem('planora_theme',dark?'dark':'light')},[dark]);const shown=notes.filter(n=>(n.title+n.body+n.tags.join(' ')).toLowerCase().includes(query.toLowerCase()));const close=()=>setModal(null);return <div className={dark?'app dark':'app'}><aside className={menu?'open':''}><div className="brand"><span>✦</span> planora</div><p className="overline">PRACOVNÝ PRIESTOR</p>{nav.map(([x,I])=><button className={page===x?'nav active':'nav'} onClick={()=>{setPage(x);setMenu(false)}} key={x}><I size={18}/>{x}</button>)}<div className="asideBottom"><button className="nav"><Settings size={18}/>Nastavenia</button><button className="nav" onClick={()=>setDark(!dark)}>{dark?<Sun size={18}/>:<Moon size={18}/>} {dark?'Svetlý režim':'Tmavý režim'}</button><div className="profile"><div className="avatar">P</div><div><strong>Používateľ</strong><small>Osobný priestor</small></div></div></div></aside><main><header><button className="mobileMenu" aria-label="Otvoriť menu" onClick={()=>setMenu(!menu)}><Menu/></button><div><span className="eyebrow">{new Date().toLocaleDateString('sk-SK',{weekday:'long',day:'numeric',month:'long'}).toUpperCase()}</span><h1>{page==='Prehľad'?'Dobré ráno.':page}</h1></div><div className="headerActions"><div className="search"><Search size={17}/><input aria-label="Hľadať" placeholder="Hľadať..." value={query} onChange={e=>setQuery(e.target.value)}/></div><button className="iconBtn" onClick={()=>setDark(!dark)} aria-label="Prepnúť tému">{dark?<Sun size={18}/>:<Moon size={18}/>}</button><button className="avatar" aria-label="Profil">P</button></div></header>{page==='Prehľad'?<Dashboard tasks={tasks} notes={notes} setPage={setPage}/>:page==='Úlohy'?<Tasks tasks={tasks} setTasks={setTasks} edit={x=>setModal({kind:'task',item:x})} add={()=>setModal({kind:'task'})}/>:page==='Poznámky'?<Notes notes={shown} setNotes={setNotes} edit={x=>setModal({kind:'note',item:x})} add={()=>setModal({kind:'note'})} query={query}/>:<Calendar tasks={tasks}/>}<MobileNav page={page} setPage={setPage}/></main>{modal&&<Modal data={modal} close={close} saveTask={x=>setTasks(ts=>ts.some(t=>t.id===x.id)?ts.map(t=>t.id===x.id?x:t):[...ts,x])} saveNote={x=>setNotes(ns=>ns.some(n=>n.id===x.id)?ns.map(n=>n.id===x.id?x:n):[...ns,x])}/>}</div>}
function Dashboard({tasks,notes,setPage}:{tasks:Task[];notes:Note[];setPage:(p:Page)=>void}){const open=tasks.filter(t=>!t.done);return <><section className="hero"><div><span className="pill">✦ TVOJ DEŇ</span><h2>Malé kroky,<br/><em>veľké výsledky.</em></h2><p>Usporiadaj si myšlienky a sústreď sa na to, čo je dôležité.</p></div><div className="orb">✦</div></section><div className="stats"><Stat label="Rozpracované úlohy" value={open.length} sub="na dnes"/><Stat label="Dokončené" value={tasks.filter(t=>t.done).length} sub="celkovo"/><Stat label="Poznámky" value={notes.length} sub="v priestore"/></div><div className="grid two"><Panel title="Najbližšie úlohy" action="Zobraziť všetky" onClick={()=>setPage('Úlohy')}><TaskList tasks={open.slice(0,3)}/></Panel><Panel title="Nedávne poznámky" action="Všetky poznámky" onClick={()=>setPage('Poznámky')}><div className="notePreview">{notes.slice(0,2).map(n=><div className="miniNote" key={n.id}><span className="noteDot">✦</span><div><strong>{n.title}</strong><p>{n.body}</p><small>{n.tags.map(t=>'#'+t).join(' ')}</small></div></div>)}</div></Panel></div></>}
function Stat(p:{label:string;value:number;sub:string}){return <div className="stat"><span>{p.label}</span><strong>{p.value}</strong><small>{p.sub}</small></div>}function Panel(p:{title:string;action:string;onClick:()=>void;children:React.ReactNode}){return <section className="panel"><div className="panelHead"><h3>{p.title}</h3><button onClick={p.onClick}>{p.action} →</button></div>{p.children}</section>}
function TaskList({tasks}:{tasks:Task[]}){return <div className="taskList">{tasks.length?tasks.map(t=><div className="task" key={t.id}><div><strong className={t.done?'done':''}>{t.title}</strong><small>{t.due} <b className={'priority '+t.priority}>{t.priority}</b></small></div></div>):<div className="empty">Žiadne úlohy. Uži si voľný čas ✦</div>}</div>}
function Tasks({tasks,setTasks,edit,add}:{tasks:Task[];setTasks:React.Dispatch<React.SetStateAction<Task[]>>;edit:(t:Task)=>void;add:()=>void}){return <><div className="toolbar"><p className="muted">{tasks.filter(t=>!t.done).length} otvorené · {tasks.filter(t=>t.done).length} dokončené</p><button className="primary" onClick={add}><Plus size={17}/> Nová úloha</button></div><section className="panel"><div className="taskList">{tasks.map(t=><div className="task taskRow" key={t.id}><button className={'check '+(t.done?'checked':'')} aria-label="Označiť úlohu" onClick={()=>setTasks(ts=>ts.map(x=>x.id===t.id?{...x,done:!x.done}:x))}>{t.done&&'✓'}</button><div className="taskInfo"><strong className={t.done?'done':''}>{t.title}</strong><small>{t.due} <b className={'priority '+t.priority}>{t.priority}</b></small></div><button className="rowAction" aria-label="Upraviť úlohu" onClick={()=>edit(t)}><Pencil size={15}/></button><button className="rowAction" aria-label="Zmazať úlohu" onClick={()=>confirm('Naozaj chcete zmazať túto úlohu?')&&setTasks(ts=>ts.filter(x=>x.id!==t.id))}><Trash2 size={15}/></button></div>)}</div></section></>}
function Notes({notes,setNotes,edit,add,query}:{notes:Note[];setNotes:React.Dispatch<React.SetStateAction<Note[]>>;edit:(n:Note)=>void;add:()=>void;query:string}){return <><div className="toolbar"><p className="muted">{query?`Výsledky pre „${query}“`:'Tvoje myšlienky na jednom mieste'}</p><button className="primary" onClick={add}><Plus size={17}/> Nová poznámka</button></div><div className="notesGrid">{notes.map(n=><article className={n.pinned?'noteCard pinned':'noteCard'} key={n.id}><div className="noteTop"><span className="noteIcon">✦</span><div><button className="delete" aria-label="Upraviť poznámku" onClick={()=>edit(n)}><Pencil size={16}/></button><button className="delete" aria-label="Vymazať poznámku" onClick={()=>confirm('Naozaj chcete zmazať túto poznámku?')&&setNotes(ns=>ns.filter(x=>x.id!==n.id))}><Trash2 size={16}/></button></div></div><h3>{n.title}</h3><p>{n.body}</p><div className="tags">{n.tags.map(t=><span key={t}>#{t}</span>)}<button aria-label="Pripnúť" onClick={()=>setNotes(ns=>ns.map(x=>x.id===n.id?{...x,pinned:!x.pinned}:x))}>{n.pinned?<Pin size={14}/>:<PinOff size={14}/>}</button></div></article>)}</div></>}
function Calendar({tasks}:{tasks:Task[]}){const[cursor,setCursor]=useState(new Date()),year=cursor.getFullYear(),month=cursor.getMonth(),first=new Date(year,month,1).getDay()||7,days=new Date(year,month+1,0).getDate(),today=new Date().toISOString().slice(0,10);return <section className="panel calendar"><div className="calHead"><button className="iconBtn" aria-label="Predchádzajúci mesiac" onClick={()=>setCursor(new Date(year,month-1,1))}><ChevronLeft/></button><h2>{cursor.toLocaleDateString('sk-SK',{month:'long',year:'numeric'})}</h2><button className="todayBtn" onClick={()=>setCursor(new Date())}>Dnes</button><button className="iconBtn" aria-label="Nasledujúci mesiac" onClick={()=>setCursor(new Date(year,month+1,1))}><ChevronRight/></button></div><div className="week">{['Po','Ut','St','Št','Pi','So','Ne'].map(x=><b key={x}>{x}</b>)}</div><div className="days">{Array.from({length:first-1},(_,i)=><div className="day mutedDay" key={'e'+i}/>).concat(Array.from({length:days},(_,i)=>{const date=`${year}-${String(month+1).padStart(2,'0')}-${String(i+1).padStart(2,'0')}`;return <div className={date===today?'day current':'day'} key={date}><span>{i+1}</span>{tasks.filter(t=>t.due===date).map(t=><small className="event" key={t.id}>{t.title}</small>)}</div>}))}</div></section>}
function MobileNav({page,setPage}:{page:Page;setPage:(x:Page)=>void}){return <nav className="mobileNav">{nav.map(([x,I])=><button className={page===x?'active':''} onClick={()=>setPage(x)} key={x}><I size={18}/><span>{x}</span></button>)}</nav>}
function Modal({data,close,saveTask,saveNote}:{data:{kind:'task'|'note';item?:Task|Note};close:()=>void;saveTask:(t:Task)=>void;saveNote:(n:Note)=>void}){const old=data.item as Task&Note|undefined;const[title,setTitle]=useState(old?.title||''),[body,setBody]=useState(data.kind==='note'?(old as Note)?.body||'':''),[tags,setTags]=useState(data.kind==='note'?((old as Note)?.tags||[]).join(', '):''),[due,setDue]=useState(data.kind==='task'?(old as Task)?.due||new Date().toISOString().slice(0,10):''),[priority,setPriority]=useState<Priority>(data.kind==='task'?(old as Task)?.priority||'Stredná':'Stredná');useEffect(()=>{const f=(e:KeyboardEvent)=>e.key==='Escape'&&close();document.addEventListener('keydown',f);return()=>document.removeEventListener('keydown',f)},[]);return <div className="modalBack" onMouseDown={e=>e.target===e.currentTarget&&close()}><form className="modal" onSubmit={e=>{e.preventDefault();if(!title.trim())return;const id=old?.id||crypto.randomUUID();data.kind==='task'?saveTask({id,title,due,priority,done:(old as Task)?.done||false}):saveNote({id,title,body,tags:tags.split(',').map(x=>x.trim()).filter(Boolean),pinned:(old as Note)?.pinned||false,updated:new Date().toISOString()});close()}}><button type="button" className="close" aria-label="Zavrieť" onClick={close}><X/></button><span className="pill">{old?'UPRAVIŤ':'NOVÉ'} {data.kind==='task'?'ÚLOHU':'POZNÁMKU'}</span><h2>{data.kind==='task'?'Na čom chceš pracovať?':'Zachyť svoju myšlienku.'}</h2><label htmlFor="title">Názov</label><input id="title" autoFocus value={title} onChange={e=>setTitle(e.target.value)} required/>{data.kind==='task'?<><label htmlFor="due">Termín</label><input id="due" type="date" value={due} onChange={e=>setDue(e.target.value)} required/><label htmlFor="priority">Priorita</label><select id="priority" value={priority} onChange={e=>setPriority(e.target.value as Priority)}><option>Nízka</option><option>Stredná</option><option>Vysoká</option></select></>:<><label htmlFor="body">Poznámka</label><textarea id="body" value={body} onChange={e=>setBody(e.target.value)}/><label htmlFor="tags">Tagy <small>(oddeľ čiarkou)</small></label><input id="tags" placeholder="práca, nápad" value={tags} onChange={e=>setTags(e.target.value)}/></>}<button className="primary" type="submit">Uložiť</button></form></div>}
createRoot(document.getElementById('root')!).render(<App/>);
